# Secrets — never commit these
.env
.env.*
*.pem
*.key
*.p12
*.pfx
id_rsa*
*credentials*

# OS / editor noise
.DS_Store
Thumbs.db
*.swp
.vscode/
.idea/

# Build output (none today; here so it stays that way)
node_modules/
dist/
